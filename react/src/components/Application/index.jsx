var React = require('react');

require('./styles.less');

var Application = React.createClass({
  render: function() {
    return (
      <div>
        lolvids
      </div>
    );
  }
});

module.exports = Application;
